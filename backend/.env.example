# Supabase PostgreSQL connection (use the asyncpg driver format)
DATABASE_URL=postgresql://postgres:[password]@[host]:[port]/[database]

# Gemini API Key
GEMINI_API_KEY=your-gemini-api-key

# JWT Secret (generate a random string)
JWT_SECRET=randon-string

# Upload directory
UPLOAD_DIR=uploads
